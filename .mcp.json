{
  "mcpServers": {
    "lcm": {
      "command": "node",
      "args": ["${CLAUDE_PLUGIN_ROOT}/dist/mcp-server/index.js"],
      "env": {
        "LCM_DB_PATH": "${CLAUDE_PLUGIN_DATA}/lcm.db"
      }
    }
  }
}
